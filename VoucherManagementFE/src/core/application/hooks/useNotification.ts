"use client";

// Re-export from NotificationContext for backward compatibility
export { useNotification, NotificationProvider } from "../contexts/NotificationContext";
export type { Notification, NotificationType } from "../contexts/NotificationContext";
